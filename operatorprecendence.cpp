#include<bits/stdc++.h>
using namespace std;

int main(){
    int a=45, b=54;
    int c = ((((a*5) + b) -45) +84);
    cout<<c;
    // ab + - ki precedence same hote hai toh associativity.
    //toh ab aise mai aate hai operator prefrence 
    // website pe hamesha prefer karna yaad karne ki zarurat nahi hai 
    // jaise ab * / + - ki assciativity left to right ki hai. 
}